# Local analysis workspace — do not commit blobs to git.
# Package ships starting FLStocks in ../inst/extdata/ instead.
# See README.md for the data policy (web / generated / shipped).

*
!.gitignore
!README.md
